

userid: <%= user.id %><br>
email: <%= user.email %><br>

<%= 'sign_in_count'.humanize  %><br>
<%= user.sign_in_count %><br>

<%= 'current_sign_in_at'.humanize  %>
<%= user.current_sign_in_at %>

<%= 'last_sign_in_at'.humanize  %>
<%= user.last_sign_in_at %><br>

<%= 'current_sign_in_ip'.humanize  %>
<%= user.current_sign_in_ip %>

<%= 'last_sign_in_ip'.humanize  %>
<%= user.last_sign_in_ip %><br>

<%= 'confirmed_at'.humanize  %>
<%= user.confirmed_at %><br>

<%= 'confirmation_sent_at'.humanize  %>
<%= user.confirmation_sent_at %><br>

<%= 'unconfirmed_email'.humanize  %>
<%= user.unconfirmed_email %><br>

<%= 'confirmation_token'.humanize  %>
<%= user.confirmation_token %><br>

# Erorr it Deletes all users 
<%= link_to "Destroy", user, 
    class: "whitespace-nowrap inline-flex items-center justify-center px-4 py-2 border border-transparent rounded-md shadow-sm text-base font-medium text-white bg-gray-600 hover:bg-gray-400",
    data:{turbo_method: :delete, turbo_confirm: "Are you sure?" } 
%>

<hr>


<div id="<%= dom_id post %>">
  <p class="my-5">
    <strong class="block font-medium mb-1">Title: <%= post.title %></strong>
  </p>

  <p>
    <%# 
    Not working as should example "10 min ago" 
    importmap.rb - local-time not imported 
    './bin/importmap pin local-time' is not working on windows 
    %>
    Posted: <%= time_tag post.created_at, "data-local": "time-ago" %> 
  </p>

  <p>
    <%= pluralize post.comments.count, "comment" %>
  </p>

  <p class="my-5">
    <strong class="block font-medium mb-1">Content:</strong>
    <%= post.content %>
  </p>

  <% if action_name != "show" %>
    <%= link_to "Show this post", post, class: "rounded-lg py-3 px-5 bg-gray-100 hover:bg-gray-200 inline-block font-medium" %>
    <%= link_to 'Edit this post', edit_post_path(post), class: "rounded-lg py-3 ml-2 px-5 bg-gray-100 hover:bg-gray-200 inline-block font-medium" %>
    <hr class="mt-6">
  <% end %>
</div>
